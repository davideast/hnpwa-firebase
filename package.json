{
  "name": "hnpwa-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "fs-extra": "^3.0.1",
    "lighthouse": "^2.1.0",
    "workbox-build": "^1.0.1"
  },
  "devDependencies": {
    "firebase-tools": "^3.9.1",
    "lighthouse-ci": "https://github.com/ebidel/lighthouse-ci"
  },
  "scripts": {
    "test": "echo \"No test specified\" && exit 0",
    "copy:workbox": "cp node_modules/workbox-sw/build/importScripts/* src/",
    "build:sw": "npm run copy:workbox && node build/sw.build.js",
    "build": "tsc -p functions/tsconfig.json && cp -R src/ ./public && mv ./public/index.html functions/ && npm run build:sw",
    "serve": "tsc -p functions/tsconfig.json && firebase serve --only functions,hosting"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
